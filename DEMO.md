# 60 秒演示

从当前包目录运行 `node src/cli.mjs serve`，打开 http://127.0.0.1:6400 。确定性演示不调用模型，不发送真实任务。

| 时间 | 操作 | 能看见的价值 |
| --- | --- | --- |
| 0–10 秒 | 点击“载入演示 fixture”，引用预算原话和模型建议 | 原话与建议角色明确分开 |
| 10–20 秒 | 从同一消息再选离线决定的片段 | 精确到选段，保留独立引用编号 |
| 20–30 秒 | 添加确认背景，问“引用 2 是否违反引用 1？” | 背景带来源，问题围绕选中材料 |
| 30–40 秒 | 预览正文，下载 JSON 或 Markdown | 未选的私有标记不进入共享包；导出已回读校验 |
| 40–50 秒 | 导入一个损坏文件，再重新加载页面 | 错误可见，已保存草稿仍在 |
| 50–60 秒 | 将下载的包交给另一 Agent | 原文、问题、背景、来源共同携带 |

没有验证过的发送连接时，“复制到 Codex”是手动入口。演示不宣称存在原生消息气泡按钮，不把契约 fixture 当成真实发送或模型回答。

命令行版本：`node scripts/demo.mjs`，报告及可交换文件在 `.runtime/demo/`。所有演示数据均为合成样例；演示不证明真实模型效果。
